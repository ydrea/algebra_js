import spinner from "../assets/spinner.gif";

const Spinner = () => {
  return <img src={spinner} alt="Učitavanje..." style={{ margin: "auto", display: "block" }} />;
};

export default Spinner;
